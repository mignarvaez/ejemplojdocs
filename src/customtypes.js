/**
 * User
 * @typedef {Object} User
 * @property {number} id User id
 * @property {string} name User's Name
 * @property {number | string} [age] User's age (Optional)
 * @property {boolean} isActive User state
 */

/**
 * @type {User}
 */
const miNuevoUsuario ={
    id:1,
    name:'rayn',
    age: 30,
    isActive: true
}

/**
 * @type {User}
 */
const miNuevoUsuario2 ={
    id:2,
    name:'Joe',
    age: 30,
    isActive: true
}