/**
 * Lista de edades de usuarios. Es un arreglo genérico, puede
 * tener varios tipos de datos.
 * @type {Array}
 */
const age = [19,20,30,100,{}];

/**
 * Arreglo de tipo númerico de puntajes
 * @type {Array<Number>}
 */
const scores = [10, 199.99, 299.99, 3000];

/**
 * Arreglo de números y strings
 * @type {Array<Number | string>}
 */
const numerosYCadenas = [10, 199.99, "10", "30"]